<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="bottom"
    android:orientation="vertical"
    tools:context=".scene.useless.ChatRobotActivity">

    <EditText
        android:id="@+id/chat_logs"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginBottom="10dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/list_selector_background_focused"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:gravity="top"
        android:inputType="textNoSuggestions|textMultiLine"
        android:padding="3dp"
        android:scrollbars="vertical"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/chat_msg_out"
        android:layout_gravity="right"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="5dp"
        android:gravity="center_vertical"
        android:background="@drawable/chat_item_out_focused" />

    <TextView
        android:id="@+id/chat_msg_in"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="5dp"
        android:gravity="center_vertical"
        android:background="@drawable/chat_item_in_focused" />

    <Button
        android:id="@+id/chat_button_speaking"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@android:color/holo_red_dark"
        android:textSize="50sp"
        android:text="@string/btn_title_command" />

</LinearLayout>
